### WORKFLOW:

# Aggiorno rispetto alla repo del prof

git pull upstream 2026 

# Salvo modifiche locali online

git add .
git commit -m " "
git push




# Da ricordare:

dopo aver fatto cd, docker compose up  -d
devi runnare la cella per usare ipython
